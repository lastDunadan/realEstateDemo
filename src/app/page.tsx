'use client';
import type { ReactElement } from 'react';

import { FavouriteIcon } from '@/atoms/FavouriteIcon/FavouriteIcon';

const Home = (): ReactElement => (
  <>
    <FavouriteIcon />
  </>
);

export default Home;
